# ── Rust ───────────────────────────────────────────────
/target/                     # Cargo build artefacts
/debug/                      # Optional debug dir
**/*.rs.bk                   # rustfmt backups
*.pdb                        # MSVC debug symbols
**/mutants.out*/             # cargo-mutants

# ── Node / Svelte (UI) ─────────────────────────────────
node_modules/
pnpm-lock.yaml               # generated; re-create via `pnpm install`
/ui/.svelte-kit/
/ui/dist/                    # production build
.vite/                       # Vite hot-reload cache

# ── Tauri Bundles ──────────────────────────────────────
/src-tauri/target/release/bundle/
/src-tauri/target/release/*.msi
/src-tauri/target/release/*.dmg
/src-tauri/target/release/*.AppImage
/src-tauri/target/release/*.zip

# ── IDEs & OS clutter ─────────────────────────────────
.DS_Store
Thumbs.db
.idea/                       # JetBrains (RustRover)
.vscode/

# ── Logs ───────────────────────────────────────────────
npm-debug.log*
pnpm-debug.log*
*.log
